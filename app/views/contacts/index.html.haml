%p#notice= notice
%h1 Listing Contacts
%table
  %thead
    %tr
      %th Id
      %th User
      %th Name
      %th Phone
      %th Email
      %th{:colspan => "3"}
  %tbody
    - @contacts.each do |contact|
      %tr
        %td= contact.id
        %td= "#{contact.user.name}:#{contact.user.id}" if contact.user
        %td= contact.name
        %td= contact.phone
        %td= contact.email
        %td= link_to 'Show', contact
        %td= link_to 'Edit', edit_contact_path(contact)
        %td= link_to 'Destroy', contact, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Contact', new_contact_path
